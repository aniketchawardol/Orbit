# Facility has no models of its own — it operates on catalog.ItemUnit
# and marketplace.Listing. Kept as an app for clean route/permission
# separation and the storage-cost engine (management command + services).
